<%= form_with(model:[post, post_comment], local: true) do |f| %>
  <%= f.text_area :comment, rows:'1',placeholder: "コメントをここに", class: "w-20" %><br>
  <%= f.submit "コメントを送信", class: "btn btn-success" %>
<% end %>