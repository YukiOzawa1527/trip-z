<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/admin">管理Page</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if admin_signed_in? %>
        <%= render 'admin/searches/form', url: admin_search_path %>
        <%= render 'admin/searches/tag_form', url: admin_search_path %>

        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= link_to "User一覧", admin_users_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Post一覧", admin_root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_admin_session_path, method: :delete, class: "nav-link" %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>